# GitHub Pages deployment configuration

# Build configuration for static deployment
VITE_STATIC_DEPLOYMENT=true

# GitHub Pages specific settings
# Make sure to set the correct base path if deploying to a repository subdirectory
# For example, if your repo is named 'swissApp', uncomment the next line:
# VITE_BASE_URL=/swissApp/

# Production environment
NODE_ENV=production